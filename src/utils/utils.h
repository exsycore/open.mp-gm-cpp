#pragma once

#include <string>
#include <windows.h>

std::string _utf8ansi874(const std::string &utf8);

// void _sendClientMessage(IPlayer &player, const Colour& colour, const std::string& win874Message);